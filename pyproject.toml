[project]
name = "linkedin-agent"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "anthropic>=0.40.0",
    "feedparser>=6.0.11",
    "requests>=2.32.0",
    "PyYAML>=6.0.1",
    "libsql-client>=0.3.1"
]

[tool.vercel]
entrypoint = "api.telegram_webhook:app"